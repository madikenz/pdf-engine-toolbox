fastapi==0.129.0
uvicorn[standard]==0.41.0
PyMuPDF==1.27.1
pydantic==2.12.5
pydantic-settings==2.13.1
httpx==0.28.1
httpx[http2]==0.28.1
python-dotenv==1.2.1
structlog==25.5.0
# Spreadsheet pre-processing (fit-to-width for Excel→PDF)
openpyxl==3.1.5
# Image processing
opencv-python-headless==4.13.0.92
Pillow==12.1.1
pillow-heif==1.2.1
# OCR (PaddleOCR 3.x – PP-OCRv5, CPU-only, no CUDA required)
# PaddleOCR 3.x uses .predict() API with PP-OCRv5 models for best accuracy.
# PPStructure replaced by PPStructureV3.
# NOTE: PaddlePaddle 3.3.0 has a regression breaking CPU inference via oneDNN
# (github.com/PaddlePaddle/Paddle/issues/77340). Pin to 3.2.2 until fixed.
paddlepaddle>=3.2.0,<3.3.0
paddleocr>=3.0.0,<4.0.0
paddlex[ocr]
# Testing
pytest==9.0.2
pytest-asyncio==1.3.0
